






































int f(int a);
int f2(int a, int b) {}

struct Complex
{
	int a;
}

int main()
{
	int a = (~-5++);
	int bbb = 5 ++ **(a + 5);

	int bb = !!!!a?;

	Complex myCompl;
	int f2test = myCompl.a + f2(myCompl.a, bb);
	
	int z = (5 + a?) * 2 %%%%% *a;

	int b = -5 * &a?;

	int c = a+++++b;

	int y = f2(f(f(a) * a + -2), -a * -a);
}
